// 𝖊𝖗𝖗𝖔𝖗 𝖉𝖎𝖋𝖋𝖚𝖘𝖎𝖔𝖓
// OSC Values for variable shader fun

// USE THESE IN MOST CONVERTED SHADERS
uniform vec2 mouse;

// Type of shader expected by Processing
#define PROCESSING_COLOR_SHADER

// FFT From Processing Sketch (glslvjay fork))
uniform float freq1;
uniform float freq2;
uniform float freq3;
uniform float freq4;

uniform vec4 myCol1;
uniform vec4 myCol2;

// Layer between Processing and Shadertoy uniforms
// not sure what these are yet -- testing
uniform sampler2D iChannel0;
uniform vec2 iChannel0Size;


uniform vec2 mousePressed; // (0.0,0.0) no click, (1.0,1.0) left mouse button pressed
vec4 iMouse = vec4(mouse.xy,mousePressed.xy);

//shader specific uniforms
uniform float brightness;
uniform float rotation;

whatevert the fuq we want

